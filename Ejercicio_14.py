# Entradas
Kw1 = float(input("\nDime la lectura anterior y la actual "))
Kw2 = float(input())
val = float(input("Dime el valor por kilovatio "))

# Caja negra
T = (Kw2-Kw1)* val

# Salida
print("\nEl valor total es",T ,"\n")